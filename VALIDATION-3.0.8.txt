Remnawave Reverse 3.0.8 validation
Date: 2026-08-08 UTC

Changes validated:
- SCRIPT_VERSION=3.0.8.
- Nginx Node certificate handling now targets /opt/remnanode.
- Certbot renewal/deploy hooks use the selected installation directory.
- Existing standalone Node installations can self-repair missing certificate volume mounts with backup + compose validation + rollback.
- Standalone Node readiness no longer uses curl https://SELFSTEAL_DOMAIN (invalid under REALITY fallback); it checks remnanode, fallback container, TCP/2222 and /dev/shm/nginx.sock.
- docs/src/assets/logo.webp, banner.webp and banner-black.webp were converted from mislabeled PNG bytes to real WebP files.

Validation performed:
PASS - bash -n on all 20 shell files.
PASS - static check: standalone Nginx Node passes /opt/remnanode to handle_certificates.
PASS - static check: old curl+grep public HTTPS readiness test removed from Nginx and Caddy standalone Node installers.
PASS - RU/EN repair language keys present.
PASS - mock repair test inserts both certificate mounts exactly once and is idempotent.
PASS - mock rollback test restores the original docker-compose.yml when compose validation fails.
PASS - WebP assets decode successfully as WEBP with Pillow.

Not performed in this environment:
- Live Docker Engine / Docker Compose recreation on the user's VPS.
- Live Certbot renewal against Let's Encrypt.
- Live Remnawave Panel <-> Node connection.
- Full Astro build (dependencies are not installed in this offline build container).
